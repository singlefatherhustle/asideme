# Inline styles

**Inline styles** are CSS declarations contained within a `style` attribute.

*Avoid* using inline styles if possible!

- least efficient implementation of CSS for maintenance
- breaks separation of concerns between presentation (CSS) and content (HTML)

## Code Example

```html
<h1 style="color:blue;">Welcome!</h1>
```

---

All content is proprietary and confidential.
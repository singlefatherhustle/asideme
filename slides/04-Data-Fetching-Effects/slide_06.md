# Why do we care about side effects?

Because React assumes that every component is a pure function, which means:

1. A React components always returns the same JSX when given the same props.
2. A React components minds its own business (no side effects).

---

*All content is proprietary and confidential.*

**Page 6**
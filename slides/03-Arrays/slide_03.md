# Arrays

Much like strings, an **array** is a data type that can hold a **sequence** of values. Arrays are denoted by comma-separated values inside square brackets.

```js
const numbers = [3, 2, 1, 1, 0];
const vegetables = ["carrots", "cabbage", "lettuce"];
```

---

*All content is proprietary and confidential.*
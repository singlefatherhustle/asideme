# Git

We will be using **Git** as our version control system. Git helps us track the history of all the changes to files in a folder. A folder that is being tracked by Git is called a **repository**.

Running `git init` in a folder will initialize that folder as a repository i.e. tell Git to start tracking that folder.

## Visual Elements

- Git logo (blue diamond with white branching nodes icon)
- Folder icon (blue folder illustration)

---

*All content is proprietary and confidential.*
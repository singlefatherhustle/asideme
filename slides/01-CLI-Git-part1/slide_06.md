# Commit

We then **commit** the changes in the staging area. This creates a permanent record in the history of the repository.

## Definitions

**commit (verb)** - to record an event in the history of a repository

**commit (noun)** - an event in the history of a repository

## Diagram

```
staging area
┌─────────────────────────────────────────┐
│                                         │
│  a1 ─────────git commit────────→ a1    │
│ (green)                        (white) │
│                                         │
└─────────────────────────────────────────┘
```

The diagram shows:
- A green circle labeled "a1" on the left (in staging area)
- An arrow labeled "git commit" pointing to the right
- A white circle labeled "a1" on the right (in repository history)
- Both contained within a trapezoid labeled "staging area"

---

*All content is proprietary and confidential.*
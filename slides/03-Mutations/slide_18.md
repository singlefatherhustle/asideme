# What about request headers?

A **request header** is a key:value pair that communicates *metadata* about the request.

For example, servers want to know the *type* of data being sent in the body of a request.

A request with a JSON body will need this header:

```
"Content-Type" : "application/json"
```

---

*All content is proprietary and confidential.*

Page 15
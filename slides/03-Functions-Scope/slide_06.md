# Parameters

A function can also take input! A **parameter** is a *variable* that a function uses to store that input. These are defined when the function is declared, and can be used like any other variable in the function body.

```js
function greet(name){
  console.log(`Hi! My name is ${name}!`);
}
```

---

*All content is proprietary and confidential.*

**Page 6**
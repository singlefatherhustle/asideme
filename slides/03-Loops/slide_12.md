# Parts of a definite loop

Definite loops are so common that we have special names for the parts of the loop that control how many times the loop runs.

```js
let i = 0; // Initialization
while (i < 3){ // Condition
    console.log("Counting..." + i);
    i = i + 1; // Afterthought
}
```
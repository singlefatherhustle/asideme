# Review

## Why does a component need to be re-rendered when its state changes?

Even if the data changes on the JavaScript side, the *user* will not see that change until it is reflected in the document.

Re-rendering a component will update the DOM, which will then update the document.

---

*All content is proprietary and confidential.*
# Review

## What does it mean to *render* a component?

Rendering a component means **calling** the function to get an HTML element which is then put into the DOM. Once it is in the DOM, the browser's render engine will update the document.

---

*All content is proprietary and confidential.*

**Page 6**
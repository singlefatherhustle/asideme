# Interface

We can also use a **different** *interface*: the **Command Line Interface**!

## Diagram: User Interaction Flow

```
user → CLI → computer → directory structure
```

### Components:
- **User** (stick figure icon)
- **CLI** (Command Line Interface - pink/magenta terminal window icon with `> _` prompt)
- **computer** (blue robot/device icon)
- **directory structure** (blue folder/file system icon)

---

*All content is proprietary and confidential.*
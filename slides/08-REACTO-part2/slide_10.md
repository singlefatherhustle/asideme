# O = Optimize your code

**~10-15 minutes**

Optimization is the very last step. A working suboptimal approach is better than a broken *potentially* optimal approach.

## What is the time and space analysis of your solution?

For every element in array A, we need to look at every element in array B, so the time complexity is **O(n*m) or O(n²)**.

The space complexity is **O(1)**, since we only make a single counter variable.

## Can you do better than that?

**Yes!**

---

*All content is proprietary and confidential.*

**Page 10**
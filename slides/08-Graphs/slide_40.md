# Which one should you use?

How do you know which data structure to use to implement a graph? As always, it depends!

A heuristic you can use:

- Adjacency matrix is better for frequently working with *edges* in a **dense** graph.
- Adjacency list is better for frequently working with *vertices* in a **sparse** graph.

---

*All content is proprietary and confidential.*

Page 28
# Anonymous functions

Function expressions are often called **anonymous functions** because they have no name. These are commonly combined with higher-order functions.

```js
repeat(() => console.log("hello"), 20);
repeat(function(){ console.log("goodbye"); }, 30);
```

---

All content is proprietary and confidential.
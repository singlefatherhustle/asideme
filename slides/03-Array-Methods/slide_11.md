# Recap

- JavaScript functions are **first-class**.
- **Arrow functions** use different syntax to write a **function expression**.
- A **higher-order** function either takes a function as input or returns a function as output.
- A **callback function** is a first-class function passed as an argument into a higher-order function.

---

All content is proprietary and confidential.

**Page: 11**
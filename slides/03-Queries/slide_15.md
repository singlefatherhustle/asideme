# Recap

1. A **client** wants an array of paintings from a **server**.
2. The client reads the documentation of a server's exposed **RESTful API** to find the **GET /paintings** **endpoint**.
3. The client sends a **request** with the appropriate **method** and **path**.
4. The server sends back a **response** with **status code** 200 and **status message** OK.
5. The response **body** contains the array of paintings.

---

All content is proprietary and confidential.
# Is the user logged in?

The token can be used as an indicator for whether or not the user is logged in.

For example,
- if the token exists, then we render a "Log out" link
- if the token does not exist, then we render a "Log in" link

---

All content is proprietary and confidential.

Page 8
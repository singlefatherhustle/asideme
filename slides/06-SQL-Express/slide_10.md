# Delete records in a table

Similarly, you can delete *all* the records in a table, but you usually want to *filter* for specific records that you want to delete.

```sql
DELETE FROM table_name;
```

```sql
DELETE FROM menu
WHERE id = 1;
```

---

*All content is proprietary and confidential.*
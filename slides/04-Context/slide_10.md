# Create a new context

How do we use context? First, we need to create a new context with `createContext`.

You can think of context as an empty box.

## Diagram

A rectangular box with the label "context" inside a cloud-like shape at the top center of the box.

---

*All content is proprietary and confidential.*

**Page: 7**
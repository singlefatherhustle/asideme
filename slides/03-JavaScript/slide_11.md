# ES vs JS

ECMAScript is a *specification*, not a *programming language*!

Different companies and organizations follow the ECMAScript blueprint in order to build **JavaScript engines**, which are responsible for actually reading and executing JavaScript.

That is why ECMAScript will *always* be ahead of JavaScript.

---

*All content is proprietary and confidential.*
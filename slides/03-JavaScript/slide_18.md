# Box analogy

You can think of a variable like a box.
A variable is a labeled box. The value is the thing *inside* the box.

A variable IS NOT the value!

## Diagram

```
variable (labeled box containing "lunch") → value (sandwich image)
```

**variable** label: `lunch`

**value** (contents): sandwich image

---

*All content is proprietary and confidential.*
# Browser

A browser has a **rendering engine**, which is responsible for parsing HTML to render a document, and it also has a **JavaScript engine**, which is responsible for reading and executing JavaScript code.

## Browser Architecture Diagram

```
┌─────────────────────────────────────┐
│         browser                     │
│    (globe icon at top)              │
├──────────────┬──────────────────────┤
│              │                      │
│ rendering    │    JS engine         │
│ engine       │                      │
│ (orange      │    (orange           │
│  engine icon)│     engine icon)     │
└──────────────┴──────────────────────┘
```

---

*All content is proprietary and confidential.*
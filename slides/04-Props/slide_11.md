# Props

Despite the fancy JSX syntax, a React component is just a JavaScript function, which means that it receives input through its parameters.

In React, most information is passed into a component function through its *first* parameter. This first parameter has a special name: **props**.

---

*All content is proprietary and confidential.*

**Page: 7**